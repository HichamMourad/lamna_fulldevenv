- name: Configure Azure Database Services
  hosts: localhost
  tasks:
    - name: Configure Azure Database Services
      debug:
        msg: "Configuring Azure Database Services..."

    - pause:
        seconds: 13
        prompt: "Azure Database Services configuration is complete!"
